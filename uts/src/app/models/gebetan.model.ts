export class Gebetan{
    constructor(
        public id: number,
        public name: string,
        public description: string,
        public gender: number, // 0 = male, 1 = female
        public picture: string
    ) {}
}
